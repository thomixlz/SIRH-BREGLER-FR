<form method="post" action="{{ path('app_types_absences_delete', {'id': types_absence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_absence.id) }}">
    <button class="btn">Delete</button>
</form>
