{% extends 'base.html.twig' %}

{% block title %}TypesAbsences index{% endblock %}

{% block body %}
    <h1>TypesAbsences index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for types_absence in types_absences %}
            <tr>
                <td>{{ types_absence.id }}</td>
                <td>{{ types_absence.libelle }}</td>
                <td>
                    <a href="{{ path('app_types_absences_show', {'id': types_absence.id}) }}">show</a>
                    <a href="{{ path('app_types_absences_edit', {'id': types_absence.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_types_absences_new') }}">Create new</a>
{% endblock %}
