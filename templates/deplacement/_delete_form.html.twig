<form method="post" action="{{ path('app_deplacement_delete', {'id': deplacement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deplacement.id) }}">
    <button class="btn">Delete</button>
</form>
