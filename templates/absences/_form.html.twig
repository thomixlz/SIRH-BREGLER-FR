{{ form_start(form) }}

    <div class="col-12 champ-form">
        <h3>Raison de l'absence :</h3>
        {{ form_row(form.type, { 'attr': { 'class': 'js-example-basic-multiple-limit form-control', 'placeholder': 'Equipe' } }) }}
    </div>


    <div class="d-flex">
        <div class="col-6 champ-form">
            <h3>Date de début :</h3>
            {{ form_row(form.dateDebut, { 'attr': { 'class': 'form-control', 'placeholder': 'Date de naissance' } }) }}
        </div>
        <div class="col-6 champ-form">
            <h3>Date de fin :</h3>
            {{ form_row(form.dateFin, { 'attr': { 'class': 'form-control', 'placeholder': 'Date de naissance' } }) }}
        </div>
    </div>

    <div class="col-12 champ-form container-form">
        <h3>Raison :</h3>
        {{ form_row(form.raison, { 'attr': { 'class': 'form-control', 'placeholder': 'Nom' } }) }}
    </div>

    <div class="col-12 champ-form container-form mt-3 mb-5">
        
            <h3>Document :</h3>
    
           
    
        <div class="d-flex">  
            {{ form_row(form.document, { 'attr': {'placeholder': 'Document', 'class': 'mr-3' } }) }}
            {# {% if absence.document %}
                <a class="mb-3" style="color:white" href="{{ asset('uploads/absences/' ~ absence.document) }}" target="_blank"><i class="fa-solid fa-eye"></i> Voir le document</a>
            {% endif %} #}
        </div>
    </div>

    <div class="col-12 d-flex justify-content-center align-items-center">
        <div class="modal-submit d-flex justify-content-center">
            <button type="submit" class="btn-crud">Enregistrer</button>
        </div>
    </div>



    {#  #}

    <div class="d-none">
        {{ form_row(form.createdAt, { 'attr': { 'class': 'form-control', 'placeholder': 'Nom' } }) }}
    </div>

    <div class="d-none">
        
        {{ form_row(form.updatedAt, { 'attr': { 'class': 'form-control', 'placeholder': 'Nom' } }) }}
    </div>

    <div class="d-none">
        
        {{ form_row(form.etat, { 'attr': { 'class': 'form-control', 'placeholder': 'Nom' } }) }}
    </div>

    <!-- Ckediotr -->
    <script src="https://cdn.ckeditor.com/4.22.1/full/ckeditor.js"></script>

    <script>
    CKEDITOR.replace('absences_raison', {customConfig: '{{ asset("ckeditor_config.js") }}'});
    </script>







    {# <div class="col-12 champ-form container-form">
        <h3>Raison :</h3>
        {{ form_row(form.raison, { 'attr': { 'class': 'form-control', 'placeholder': 'Nom' } }) }}
    </div>

    <div class="col-12 champ-form container-form">
        <h3>Raison :</h3>
        {{ form_row(form.raison, { 'attr': { 'class': 'form-control', 'placeholder': 'Nom' } }) }}
    </div> #}
    
    
{{ form_end(form) }}
